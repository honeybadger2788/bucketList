<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bucket List</title>
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('./partials/header') %>
    <main>
        <section class="new">
            <form action="/create" method="POST">
                <input type="text" name="title" placeholder="Título">
                <textarea name="text" id="text" cols="30" placeholder="Añade una nota..."></textarea>
                <button type="submit">Crear</button>
            </form>
        </section>
        <section class="list">
            <% for (let i = 0; i < listadoNotas.length; i++) {%>
                <div class="note">
                    <h2><%= listadoNotas[i].title %> </h2>
                    <p><%= listadoNotas[i].text %> </p>
                    <div class="edit">
                        <a href="/<%= listadoNotas[i].id %>/edit"><i class="far fa-edit"></i></a>
                        <form action="/<%=listadoNotas[i].id%>/delete?_method=DELETE" method="POST">
                            <button type="submit"><i class="far fa-trash-alt"></i></button>
                        </form>
                    </div>
                </div>
            <% } %> 
        </section>
    </main>
</body>
</html>